/* ============================================================
   THESMALLBOOK — SPONSORS (sponsors.js) · v248
   bookId -> array of sponsor display names shown on that book page
   as "This breakdown was fuelled by {name}".
   No backend: you (founder) add names here during your weekly
   update, after a reader pays via the Fuel flow. See docs/SPONSOR-SETUP.md.
   Keep names exactly as readers want them shown (or "A reader").
   ============================================================ */
window.TSB_SPONSORS = {
  // "atomic-habits": ["Priya M.", "Rahul S."],
  // "deep-work": ["A reader"]
};
